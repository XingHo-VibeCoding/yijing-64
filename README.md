# 易经六十四卦

《易经》六十四卦的学习与整理项目。

## 项目目标

- 系统整理六十四卦的基本信息：卦名、卦符、卦序
- 逐卦记录卦象、卦辞、爻辞与个人理解
- 形成一份可检索、可长期维护的学习资料

## 目录结构

```
.
├─ README.md             # 项目说明（本文件）
├─ .gitignore            # 忽略规则，保护本地敏感文件不上传
├─ index.html            # 前端入口
├─ package.json          # 依赖与脚本
├─ vite.config.js
├─ data/
│  ├─ 64卦总表.csv       # 六十四卦索引（源数据）
│  ├─ 64卦.json          # 64 卦：卦名 / 卦符 / 上下卦 / 六爻 + 卦辞 + 象辞 + 吉凶平
│  └─ 64卦-爻辞.json     # 384 条爻辞（爻题 / 原文 / 小象）
├─ docs/
│  ├─ research.md        # 需求研究：目标用户、竞品对比、25 天最小版本、本期不做
│  ├─ PRD.md             # 产品需求文档：功能详述、验收标准、砍掉的功能及理由
│  ├─ TECH_DESIGN.md     # 技术方案：技术路线、数据流图、数据库设计、成本控制
│  └─ 01-学习笔记.md      # 逐卦笔记模板
└─ src/
   ├─ main.jsx           # 应用入口
   ├─ App.jsx            # 总表页 + 单卦详情页 + 起卦逻辑 + 存档判定
   ├─ CastingAnimation.jsx # 起卦动画（太极 / 三圈爻 / 虚圆 / 水墨）
   ├─ CastResult.jsx     # 起卦结果页（三层解读 / 两个交互键 / 收藏 / 边界声明）
   ├─ Records.jsx        # F8 过往起卦记录与收藏
   ├─ storage.js         # 本地存储层（接口按云端形状设计，将来只换实现）
   ├─ HexagramFigure.jsx # 卦象图组件（六爻，从下往上渲染）
   ├─ SourceTag.jsx      # 来源标注（只有两类：《周易》原文 / 本项目的理解）
   ├─ index.css
   └─ casting.css
```

## 技术栈

| 层 | 选型 | 备注 |
| --- | --- | --- |
| 前端 | React 19 + Vite 8 | 构建后为纯静态文件 |
| 卦数据 | 项目内 JSON，**打包进前端** | 不走网络、不查数据库（数据量小且不变） |
| 数据库 | CloudBase PostgreSQL | 只存用户数据：起卦记录 / 收藏 / 学习进度 |
| 权限 | PostgreSQL RLS 行级权限 | 数据库层强制「只能碰自己的行」 |
| 身份 | CloudBase 匿名登录 | 打开即用，不是登录页 |
| 后端接口 | **PostgREST 自动生成，本期不写云函数** | 建表即得 REST 接口 |
| 托管 | CloudBase 静态网站托管 | HTTPS + CDN |
| 成本 | **只用免费额度** | 不开按量计费、不设预置并发 |

> **两条架构主张**：① **卦数据不进数据库**（64 卦约 23KB、永不变更，放数据库只会多一次网络往返、多一份计费）；② **本期不写云函数**（少一层就少一个故障点和一份计费项）。
>
> 技术路线的完整比较、数据流图、错误处理与迁移注意事项见 [`docs/TECH_DESIGN.md`](docs/TECH_DESIGN.md)。

## 本地运行

```bash
npm install          # 首次
npm run dev          # 开发服务器 → http://localhost:5173
npm run build        # 构建 → dist/（纯静态，可直接托管）
npm run preview      # 预览构建结果
```

> **本机注意（2026-09-22 实测）**：这台机器的执行沙箱**不允许删除已存在的文件**，而 `vite` 启动时会先
> 清空 `node_modules/.vite/deps` → 直接跑 `npm run dev` 会报
> `[safe-delete] 操作失败 ... Error during a 'trash' operation`。
> 绕过办法是**换一个缓存目录**（用一份不进仓库的临时配置）：
>
> ```bash
> npx vite --config .workbuddy/vite.dev7.config.mjs
> ```
>
> 同理 `npm run build` **第二次会失败**（要清 `dist/`），验证构建改用：
> `npx vite build --outDir .workbuddy/dist-checkN --emptyOutDir`。

## 产品定位与边界（对齐 PRD v1.4）

### 主打功能

> **情绪价值 + 学习兴趣引导。**

不是知识库（全本原文到处都有），而是**让人愿意打开、并且愿意再打开**：

```
一键起卦（愿意打开）→ 读三层解读 → 每日第一卦自动存档（愿意再打开）→ 回看时进总表深读
```

### 五条边界

1. **起卦保留，但卦只释义、不断事** —— 起卦是主入口（P0）；解读只讲「这一卦在说什么」，不对未来下断言、不做决策建议、不出现「宜 / 不宜」
2. **吉凶不回避，方向固定** —— 吉卦戒骄躁，凶卦劝慰「**运随时变，成事在人**」。这是价值观引导，不是运势判定
3. **原文与解读必须分层可见** —— 一眼分清哪句是《周易》、哪句是本项目的理解
4. **不宣称任何感应或灵验** —— 起卦机制是随机数（**内部定论，不在界面做技术说明**）；界面用「**成事在人，莫问前程**」表达同一层意思。不做「诚心则灵」的暗示，不统计「准不准」
5. **不采集身份信息** —— 数据用匿名标识关联，不存手机号、邮箱、IP、设备指纹，不做行为埋点

> **起卦留着——它是入口；砍掉的是「拿卦替人做决定」的那部分。**
> 详见 [`docs/PRD.md`](docs/PRD.md) 第 4 节。

> **关于「用户进度」**：打开即用，没有注册和登录。系统在后台生成一个随机匿名标识来关联进度，
> 用户换浏览器或清缓存会丢失进度（这一点在界面上明确告知）。详见 PRD §6 F6 与 §7.3。

## 进度

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| Day 1 | 建立项目骨架、初始化仓库 | 已完成 |
| Day 3 | 需求研究（research.md） | 已完成 |
| Day 4 | 产品需求文档 PRD（含验收标准） | 已完成 |
| Day 5 | 前端跑通：卦列表页 + 卦象图（React + Vite） | 已完成 |
| Day 5 | PRD v1.1 修订（改为前端 + 云端数据服务） | 已完成 |
| Day 5 | PRD v1.2 / v1.3 定位与功能定义修订 | 已完成 |
| Day 5 | 技术方案 `docs/TECH_DESIGN.md`（v2.0：含技术路线比较与数据流图） | 已完成 |
| Day 5 | 数据补全：64 条卦辞 + 64 条象辞 + 吉凶平判定 | 已完成 |
| Day 5 | 数据补全：**384 条爻辞 + 小象**（与爻线交叉验证 0 不一致） | 已完成 |
| Day 5 | **F7 起卦动画**：太极 + 三圈爻 + 虚圆 + 水墨 + 卦象浮现 | 已完成 |
| Day 5 | **F7 起卦结果页**：三层解读 + 两个交互键 + 边界声明 + 总表定位高亮 | 已完成 |
| Day 5 | **详情页接上真实数据**：卦辞 / 象辞 / 六爻爻辞 / 吉凶平 + 判定依据 | 已完成 |
| Day 5 | **F8 每日第一卦存档 + 收藏**（本地存储层，接口按云端形状设计） | 已完成 |
| 下一步 | 云端那一半：起卦记录与收藏同步到 CloudBase（**需先确认地域与计费**） | 待确认 |
| 待确认 | 云环境开通与数据落库（**需先确认地域与计费**） | 待开始 |
| Day 6 | **新建 `AGENTS.md` 规则文件**（Day 1 基线 + R1–R4 + 检查项） | 已完成 |
| Day 6 | **平台能力复核**：地域与计费查清，`TECH_DESIGN` 升 v2.4（订正一处错误估算） | 已完成 |
| Day 6 | **PRD v1.4：砍掉「白话译文」整层**（字段 / 界面 / 来源标注），`TECH_DESIGN` 同步 v2.5 | 已完成 |
| Day 7 | **MVP 本地跑通 + 运行说明存档**（新增「本地运行」段；本地服务实测 HTTP 200） | 已完成 |
| Day 7 | **G8 白话层清理**（代码与数据里去白话；来源标注由三类收为两类） | 已完成 |
| Day 7 | **总表补齐 F1 五项**（每行加「上下卦 + 卦辞」；窄屏折成卡片、不横滚） | 已完成 |
| 待做 | F1 键盘上下移动 + 回车；F2 上一卦 / 下一卦翻页 | 待开始 |
| 后续 | 卦名拼音 | 待开始 |
| 后续 | 按卦序逐卦补充个人解读 | 待开始 |

## 数据来源与校订

| 数据 | 来源 | 校验 |
| --- | --- | --- |
| 卦序 / 卦名 / 卦符 | 通行本（王弼本） | 卦符码点 U+4DC0–U+4DFF 连续 |
| 上下卦 / 六爻爻线 | 上下卦表推导 | 6 组自动校验 + **41 条外部全名比对**，0 矛盾 |
| 卦辞 / 象辞 | GitHub `sunls2/zhouyi`（通行本转录） | 与王弼本逐条核对；**修正源数据 2 处瑕疵** |
| 爻辞 / 小象 | 同上 | **384 条爻题阴阳与爻线交叉验证，0 不一致** |
| 吉 / 凶 / 平 | 按**卦辞整体处境**判定 | 每卦附判定依据，可追溯到卦辞；分布 39 吉 / 16 平 / 9 凶 |

- **不做白话解读（PRD v1.4 起）**：第三方白话有版权与准确性风险，本项目不搬运、也不自撰 —— 白话这一层整体砍掉，界面不出现白话区，也没有「待补」占位。
- 繁简转换曾把「终日**乾乾**」误转为「干干」，现已逐字校订（同批数据中「干父之蛊」「噬干肉」「鸿渐于干」均为正确简化）。

## 说明

- 本项目为个人学习整理，卦序采用**通行本（王弼本）**：上经三十卦（1–30）、下经三十四卦（31–64）。
- `.workbuddy/`、`Claw/`、`.env` 等本机私密内容已在 `.gitignore` 中排除，不会上传。
- 提交规范：`Day N｜一句话`，正文两行写「改了什么 / 加了什么」。
