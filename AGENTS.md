# AGENTS.md — 项目规则（易经六十四卦）

> 本文件是给 AI 助手的约束。**只允许追加，不允许重写既有条目。**
> 每条规则注明来源；追加的规则标注日期与归属。

**项目**：易经六十四卦学习
**技术栈**：前端 React 19 + Vite 8（纯静态）｜云端 CloudBase（PostgreSQL + PostgREST + RLS + 匿名登录 + 静态托管，**尚未开工**）

---

## 一、Day 1 基线：项目硬约束（不可动摇）

### 1. 当天只做当天任务，没说「进入下一板块」就不越界

- 用户没说「进入下一板块 / 开始下一步」时，**只完成当前板块的任务**
- 不顺手改代码、不顺手改文档、不顺手加功能、不顺手做优化
- 确实需要越界时：**先问，不先做**

### 2. 只用免费额度

- **不开通按量计费、不设置预置并发**
- 建库后第一件事是看控制台的资源点消耗，观察 24 小时的真实走势再继续开发

### 3. 不要求用户粘贴真实密钥；密钥不进仓库

- 不把任何密钥、Token 写进仓库文件或提交内容
- 前端不需要密钥（见 `docs/TECH_DESIGN.md` §12）

### 4. 不凭空编造平台命令

- 查不到的命令、参数、字段、页面路径一律标注「**待核实**」
- 宁可不写，不猜

### 5. 遇高风险动作先停下等确认

- 登录 / 授权 / 密钥 / 计费 / 删除云资源 / 覆盖数据 → **先停下来等用户确认**
- 云环境相关操作（创建资源、发布、删库）一律不代办

### 6. 每次只完成一个小步骤

- 一次只做一步；做完报告**实际结果**（命令、输出、证据），再走下一步

---

## 二、个人规则（Day 6 追加）

### R1｜生成物不进仓库根，已存在的文件不覆盖

- 临时脚本、构建校验目录、诊断与截图产物一律写 `.workbuddy/`（已在 `.gitignore`）
- 文档配图写 `docs/img/`，`.mmd` 源码与 `.png` 渲染结果**成对进仓库**
- 需要重做时**换新目录 / 新文件名**（如 `dist-check4`、`shot-xxx2.sh`），**不覆盖已存在文件**

**为什么**：仓库根只放项目本身的东西，调试残留不污染阅读；且本机沙箱**不允许覆盖已存在文件**（`PermissionError`），写死「不覆盖」能避免整轮白跑。

### R2｜面向用户的文字：简体中文 + 来源三分类

- 界面文案、文档、提交说明一律**简体中文**
- 凡出现卦的文字，必须标明来源：`text`《周易》原文 / `plain` 现代白话解读 / `ours` 本项目的理解·非原文
- 白话**不自撰、不搬运**，缺失就显示「待补」；不出现「宜 / 不宜」
- 吉卦戒骄躁；凶卦固定用「**运随时变，成事在人**」

**为什么**：这是产品四条边界在文字层面的落地（PRD §1.3）。没有这条，「顺手补一句白话」「顺手写个宜忌」就会把产品定位做歪。

### R3｜界面结论只认截图，不认数值断言

- 说「改好了」之前必须**真截图看**：先 `agent-browser set viewport 1440 900`（**默认 1254×564 会骗人**），再看实际渲染
- 文字压在动效 / 水墨之上时，必须检查**背景最浓的那一帧**还认不认得出字
- 长脚本拆成 2–3 步跑；输出被整段吞掉就**重跑**，不据此下结论

**为什么**：三条都是本项目的真实事故 —— 视口命令写错导致在小窗口里做「桌面排版」判断；结果页文字看不清，靠「宣纸底衬 + 降墨浓度」才救回；长脚本吞输出被误判成选择器写错。
