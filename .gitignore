# ============================================
# 易经六十四卦 · 忽略规则
# 作用：这里列出的文件「永远不会」被上传到 GitHub
# 改完这个文件请立刻用 git status --ignored 复核
# ============================================

# --- 第一优先级：本机私密内容，绝对不能公开 ---

# 会话记忆 / 工作日志目录（内含本机路径、仓库信息等）
.workbuddy/

# 上一个练习的独立 Git 仓库，不属于本项目
Claw/

# 沙箱删除失败留下的临时文件（无法删除，只能忽略；可手动删掉）
data/__tmp.json
data/__writetest.json
data/__*.json
# 数据合并的中间产物（正式数据已写入 data/64卦.json）
data/64卦-new.json

# 数据流图导出的旧版本（正式文件是 data-flow.png / cast-write-flow.png）
docs/img/data-flow-v2.png
docs/img/cast-write-flow-v2.png

# 本地配置文件（存数据库密码、API Key 等）
.env
.env.*
!.env.example

# 各类密钥与证书
*.key
*.pem
*.p12
*.pfx
secrets/
credentials*

# --- 依赖目录（体积大，且能从配置文件还原）---
node_modules/
vendor/
__pycache__/
*.py[cod]
venv/
.venv/

# --- 构建产物 / 缓存 / 日志 ---
dist/
build/
.cache/
*.log

# --- 本机专用启动器（内含绝对路径，不进仓库；正式启动入口是 .workbuddy\启动本地站点.cmd）---
一键启动.cmd
*.lnk

# --- 编辑器与操作系统文件 ---
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
